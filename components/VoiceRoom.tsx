'use client';

import { useEffect, useRef, useState } from 'react';
import { Copy, Headphones, LogOut, MessageCircle, Mic, MicOff, Radio, Send, Users, VolumeX, X } from 'lucide-react';
import { ChatMessage, Participant, ReactionItem, SignalPayload } from '@/types/voice-chat';

const RTC_CONFIG: RTCConfiguration = {
  iceServers: [
    { urls: ['stun:stun.cloudflare.com:3478', 'stun:stun.l.google.com:19302'] },
    { urls: ['turn:openrelay.metered.ca:80', 'turn:openrelay.metered.ca:443', 'turn:openrelay.metered.ca:443?transport=tcp', 'turns:openrelay.metered.ca:443?transport=tcp'], username: 'openrelayproject', credential: 'openrelayproject' },
  ],
  iceCandidatePoolSize: 4,
};

type Props = { roomId: string; userName: string; onLeave: () => void };
type Status = 'connecting' | 'connected' | 'disconnected' | 'failed';
const REACTIONS = ['👏', '❤️', '🔥', '😂', '🎉', '👍'];

function initiates(a: Participant, b: Participant) { return a.joinedAt !== b.joinedAt ? a.joinedAt < b.joinedAt : a.id < b.id; }
function initials(name: string) { return name.trim().slice(0, 2).toUpperCase() || '??'; }
function placeholder(id: string): Participant { return { id, name: 'کاربر', joinOrder: Number.MAX_SAFE_INTEGER, color: '#6366f1' as Participant['color'], isMuted: false, isDeafened: false, isCameraOn: false, isScreenSharing: false, isSpeaking: false, joinedAt: Date.now(), lastHeartbeat: Date.now() }; }

export default function VoiceRoom({ roomId, userName, onLeave }: Props) {
  const [self, setSelf] = useState<Participant | null>(null), [members, setMembers] = useState<Participant[]>([]), [statuses, setStatuses] = useState<Record<string, Status>>({});
  const [messages, setMessages] = useState<ChatMessage[]>([]), [text, setText] = useState(''), [chatOpen, setChatOpen] = useState(false);
  const [muted, setMuted] = useState(false), [deafened, setDeafened] = useState(false), [loading, setLoading] = useState(true), [error, setError] = useState(''), [audioUnlock, setAudioUnlock] = useState(false), [copied, setCopied] = useState(false), [floating, setFloating] = useState<ReactionItem[]>([]);
  const selfRef = useRef<Participant | null>(null), membersRef = useRef<Participant[]>([]), streamRef = useRef<MediaStream | null>(null);
  const peers = useRef(new Map<string, RTCPeerConnection>()), candidates = useRef(new Map<string, RTCIceCandidateInit[]>()), audios = useRef(new Map<string, HTMLAudioElement>());
  const lastSignal = useRef(0), stopped = useRef(false), timer = useRef<ReturnType<typeof setTimeout> | null>(null), negotiating = useRef(new Set<string>()), muteRef = useRef(false), deafenRef = useRef(false);
  const messagesEnd = useRef<HTMLDivElement>(null);

  const updateMembers = (next: Participant[]) => { const u = [...new Map(next.map(p => [p.id, p])).values()]; membersRef.current = u; setMembers(u); };
  const status = (id: string, value: Status) => setStatuses(s => ({ ...s, [id]: value }));

  const sendSignal = async (payload: SignalPayload, toId?: string) => {
    const fromId = selfRef.current?.id; if (!fromId || stopped.current) return false;
    try { const r = await fetch('/api/room/signal', { method: 'POST', headers: { 'Content-Type': 'application/json' }, cache: 'no-store', body: JSON.stringify({ roomId, fromId, toId, payload }) }); if (!r.ok) throw new Error(`signal ${r.status}`); return true; }
    catch (e) { console.error('[Voice] signaling failed', e); return false; }
  };

  const destroyPeer = (id: string) => { peers.current.get(id)?.close(); peers.current.delete(id); candidates.current.delete(id); negotiating.current.delete(id); const el = audios.current.get(id); if (el) { el.pause(); el.srcObject = null; el.remove(); } audios.current.delete(id); };
  const flushCandidates = async (id: string, pc: RTCPeerConnection) => { if (!pc.remoteDescription) return; const queued = candidates.current.get(id) || []; candidates.current.delete(id); for (const c of queued) { try { await pc.addIceCandidate(c); } catch (e) { console.warn('[Voice] ICE candidate rejected', e); } } };

  const createPeer = (remote: Participant) => {
    const old = peers.current.get(remote.id); if (old && old.signalingState !== 'closed') return old;
    const pc = new RTCPeerConnection(RTC_CONFIG); peers.current.set(remote.id, pc); status(remote.id, 'connecting');
    const stream = streamRef.current; if (!stream) throw new Error('microphone stream is not ready'); stream.getAudioTracks().forEach(track => pc.addTrack(track, stream));
    pc.onicecandidate = e => { if (e.candidate) void sendSignal({ type: 'ice-candidate', from: selfRef.current?.id || '', to: remote.id, candidate: e.candidate.toJSON() }, remote.id); };
    pc.ontrack = e => { const remoteStream = e.streams[0] || new MediaStream([e.track]); let el = audios.current.get(remote.id); if (!el) { el = document.createElement('audio'); el.autoplay = true; el.playsInline = true; el.setAttribute('aria-hidden', 'true'); document.body.appendChild(el); audios.current.set(remote.id, el); } el.srcObject = remoteStream; el.muted = deafenRef.current; el.play().then(() => setAudioUnlock(false)).catch(() => setAudioUnlock(true)); };
    pc.onconnectionstatechange = () => { const s = pc.connectionState; if (s === 'connected') status(remote.id, 'connected'); else if (s === 'connecting') status(remote.id, 'connecting'); else if (s === 'disconnected') status(remote.id, 'disconnected'); else if (s === 'failed') { status(remote.id, 'failed'); if (selfRef.current && initiates(selfRef.current, remote)) { destroyPeer(remote.id); setTimeout(() => void makeOffer(remote, true), 300); } } };
    pc.oniceconnectionstatechange = () => { if (pc.iceConnectionState === 'connected' || pc.iceConnectionState === 'completed') status(remote.id, 'connected'); else if (pc.iceConnectionState === 'checking') status(remote.id, 'connecting'); else if (pc.iceConnectionState === 'disconnected') status(remote.id, 'disconnected'); else if (pc.iceConnectionState === 'failed' && selfRef.current && initiates(selfRef.current, remote)) { destroyPeer(remote.id); setTimeout(() => void makeOffer(remote, true), 300); } };
    return pc;
  };

  const makeOffer = async (remote: Participant, restart = false) => {
    const local = selfRef.current; if (!local || stopped.current || !initiates(local, remote) || negotiating.current.has(remote.id)) return;
    let pc = peers.current.get(remote.id); if (!pc || pc.connectionState === 'failed' || pc.signalingState === 'closed') { destroyPeer(remote.id); pc = createPeer(remote); }
    negotiating.current.add(remote.id);
    try { if (pc.signalingState !== 'stable') return; const offer = await pc.createOffer({ iceRestart: restart }); await pc.setLocalDescription(offer); if (pc.localDescription) await sendSignal({ type: 'sdp-offer', from: local.id, to: remote.id, sdp: pc.localDescription }, remote.id); }
    catch (e) { console.error('[Voice] offer failed', e); destroyPeer(remote.id); }
    finally { negotiating.current.delete(remote.id); }
  };

  const handleSignal = async (payload: SignalPayload, fromId: string) => {
    const local = selfRef.current; if (!local || stopped.current || fromId === local.id) return;
    if (payload.type === 'join') { const next = membersRef.current.some(p => p.id === payload.participant.id) ? membersRef.current.map(p => p.id === payload.participant.id ? payload.participant : p) : [...membersRef.current, payload.participant]; updateMembers(next); if (initiates(local, payload.participant)) void makeOffer(payload.participant); return; }
    if (payload.type === 'leave') { updateMembers(membersRef.current.filter(p => p.id !== payload.participantId)); destroyPeer(payload.participantId); setStatuses(s => { const n = { ...s }; delete n[payload.participantId]; return n; }); return; }
    if (payload.type === 'state-update') { updateMembers(membersRef.current.map(p => p.id === payload.participantId ? { ...p, ...payload.updates } : p)); return; }
    if (payload.type === 'chat-message') { setMessages(m => m.some(x => x.id === payload.id) ? m : [...m.slice(-99), { id: payload.id, from: payload.from, senderName: payload.senderName, text: payload.text, timestamp: payload.timestamp }]); return; }
    if (payload.type === 'reaction') { const item = { id: payload.id, from: payload.from, emoji: payload.emoji, xOffset: 10 + Math.random() * 80 }; setFloating(r => [...r.slice(-12), item]); setTimeout(() => setFloating(r => r.filter(x => x.id !== item.id)), 3000); return; }
    let remote = membersRef.current.find(p => p.id === fromId);
    if (!remote) { remote = placeholder(fromId); updateMembers([...membersRef.current, remote]); }
    if (payload.type === 'ice-candidate') { const pc = peers.current.get(fromId) || createPeer(remote); if (pc.remoteDescription) { try { await pc.addIceCandidate(payload.candidate); } catch (e) { console.warn('[Voice] ICE failed', e); } } else candidates.current.set(fromId, [...(candidates.current.get(fromId) || []), payload.candidate]); return; }
    if (payload.type === 'sdp-offer') {
      let pc = peers.current.get(fromId); if (!pc || pc.signalingState === 'closed') pc = createPeer(remote);
      try { if (pc.signalingState !== 'stable') { if (initiates(local, remote)) return; await pc.setLocalDescription({ type: 'rollback' }); } await pc.setRemoteDescription(payload.sdp); await flushCandidates(fromId, pc); const answer = await pc.createAnswer(); await pc.setLocalDescription(answer); if (pc.localDescription) await sendSignal({ type: 'sdp-answer', from: local.id, to: fromId, sdp: pc.localDescription }, fromId); }
      catch (e) { console.error('[Voice] answer failed', e); destroyPeer(fromId); } return;
    }
    if (payload.type === 'sdp-answer') { const pc = peers.current.get(fromId); if (!pc || pc.signalingState !== 'have-local-offer') return; try { await pc.setRemoteDescription(payload.sdp); await flushCandidates(fromId, pc); } catch (e) { console.error('[Voice] answer apply failed', e); destroyPeer(fromId); } }
  };

  const heartbeat = async () => {
    const local = selfRef.current; if (!local || stopped.current) return;
    try { const r = await fetch('/api/room/heartbeat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, cache: 'no-store', body: JSON.stringify({ roomId, participantId: local.id, lastSignalTimestamp: lastSignal.current, updates: { isMuted: muteRef.current, isDeafened: deafenRef.current } }) }); if (!r.ok) throw new Error(`heartbeat ${r.status}`); const data = await r.json() as { signals?: Array<{ from: string; payload: SignalPayload; timestamp: number }>; currentMembers?: Participant[] }; for (const item of data.signals || []) { lastSignal.current = Math.max(lastSignal.current, item.timestamp); await handleSignal(item.payload, item.from); } const next = (data.currentMembers || []).filter(p => p.id !== local.id); updateMembers(next); for (const remote of next) if (initiates(local, remote)) { const pc = peers.current.get(remote.id); if (!pc || pc.connectionState === 'failed' || pc.connectionState === 'closed') void makeOffer(remote); } }
    catch (e) { console.warn('[Voice] heartbeat failed', e); }
    if (!stopped.current) timer.current = setTimeout(() => void heartbeat(), 1000);
  };

  useEffect(() => { stopped.current = false; let alive = true; const start = async () => { try { if (!navigator.mediaDevices?.getUserMedia) throw new Error('مرورگر شما از میکروفون پشتیبانی نمی‌کند'); const key = `aura-voice-id:${roomId}`; let id = sessionStorage.getItem(key); if (!id) { id = crypto.randomUUID(); sessionStorage.setItem(key, id); } const media = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true }, video: false }); if (!alive) { media.getTracks().forEach(t => t.stop()); return; } streamRef.current = media; const r = await fetch('/api/room/join', { method: 'POST', headers: { 'Content-Type': 'application/json' }, cache: 'no-store', body: JSON.stringify({ roomId, participantId: id, name: userName }) }); const data = await r.json() as { self?: Participant; others?: Participant[]; error?: string }; if (!r.ok || !data.self) throw new Error(data.error || 'ورود به اتاق ناموفق بود'); selfRef.current = data.self; setSelf(data.self); updateMembers(data.others || []); lastSignal.current = data.self.joinedAt - 1; setLoading(false); for (const remote of data.others || []) if (initiates(data.self, remote)) void makeOffer(remote); await heartbeat(); } catch (e) { if (!alive) return; setLoading(false); setError(e instanceof Error ? e.message : 'اتصال صوتی برقرار نشد'); } }; void start(); return () => { alive = false; stopped.current = true; if (timer.current) clearTimeout(timer.current); peers.current.forEach(pc => pc.close()); peers.current.clear(); audios.current.forEach(el => { el.pause(); el.srcObject = null; el.remove(); }); audios.current.clear(); streamRef.current?.getTracks().forEach(t => t.stop()); streamRef.current = null; const id = selfRef.current?.id; if (id) void fetch('/api/room/leave', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ roomId, participantId: id }), keepalive: true }).catch(() => {}); }; // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [roomId, userName]);

  useEffect(() => { messagesEnd.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages, chatOpen]);
  const sendChat = (e: React.FormEvent) => { e.preventDefault(); const value = text.trim(), me = selfRef.current; if (!value || !me) return; const m = { id: crypto.randomUUID(), from: me.id, senderName: me.name, text: value.slice(0, 1000), timestamp: Date.now() }; setMessages(x => [...x.slice(-99), m]); void sendSignal({ type: 'chat-message', ...m }); setText(''); };
  const sendReaction = (emoji: string) => { const me = selfRef.current; if (!me) return; const id = crypto.randomUUID(), item = { id, from: me.id, emoji, xOffset: 10 + Math.random() * 80 }; setFloating(x => [...x.slice(-12), item]); setTimeout(() => setFloating(x => x.filter(r => r.id !== id), 3000)); void sendSignal({ type: 'reaction', id, from: me.id, emoji, timestamp: Date.now() }); };
  const toggleMute = () => { const v = !muteRef.current; muteRef.current = v; setMuted(v); streamRef.current?.getAudioTracks().forEach(t => t.enabled = !v); };
  const toggleDeaf = () => { const v = !deafenRef.current; deafenRef.current = v; setDeafened(v); audios.current.forEach(el => el.muted = v); };
  const unlock = async () => { let failed = false; for (const el of audios.current.values()) { el.muted = deafenRef.current; try { await el.play(); } catch { failed = true; } } setAudioUnlock(failed); };
  const copy = async () => { try { await navigator.clipboard.writeText(window.location.href); setCopied(true); setTimeout(() => setCopied(false), 1500); } catch {} };
  const all = self ? [self, ...members] : members, connected = Object.values(statuses).filter(s => s === 'connected').length;

  return <div dir="rtl" className="voice-app min-h-screen overflow-hidden bg-[#070b16] text-white"><div className="voice-bg" aria-hidden="true"><span /><span /><span /></div><header className="glass-header relative z-20 flex h-16 items-center justify-between px-4 sm:px-7"><div className="flex items-center gap-3"><div className="brand-mark"><Radio className="h-4 w-4" /></div><div><b>AchoVocal</b><div className="mt-0.5 flex items-center gap-1 text-[10px] text-white/40">اتاق {roomId}<button onClick={() => void copy()} className="rounded p-1 hover:bg-white/10"><Copy className="h-3 w-3" /></button>{copied && <span className="text-emerald-300">کپی شد</span>}</div></div></div><div className="flex items-center gap-2"><div className="member-count"><Users className="h-3.5 w-3.5" /> {all.length}</div><button onClick={() => setChatOpen(true)} className="icon-btn"><MessageCircle className="h-4 w-4" /></button></div></header><main className="relative z-10 mx-auto flex min-h-[calc(100vh-128px)] w-full max-w-6xl flex-col px-3 py-5 sm:px-7 sm:py-7">{error && <div className="mb-4 rounded-2xl border border-rose-400/20 bg-rose-400/10 p-4 text-sm text-rose-100">{error}</div>}{audioUnlock && <button onClick={() => void unlock()} className="mb-4 w-full rounded-2xl border border-amber-300/20 bg-amber-300/10 p-3 text-sm text-amber-100">برای فعال شدن صدای دریافتی کلیک کنید</button>}<div className="mb-5 flex items-end justify-between"><div><div className="mb-1 text-[11px] font-semibold uppercase tracking-[.22em] text-indigo-300/80">Voice room</div><h1 className="text-2xl font-black sm:text-4xl">با هم صحبت کنید<span className="text-indigo-300">.</span></h1><p className="mt-1.5 text-xs text-white/40 sm:text-sm">گفتگوی صوتی گروهی، سریع و سبک.</p></div><div className="hidden rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3 sm:block"><div className="text-[10px] text-white/35">وضعیت اتصال</div><div className="mt-1 text-xs font-semibold text-emerald-300">{connected ? `${connected} اتصال صوتی فعال` : 'در انتظار اتصال صوتی'}</div></div></div>{loading ? <div className="glass-panel flex min-h-[420px] flex-1 items-center justify-center rounded-[2rem]"><div className="text-center"><div className="loader-ring mx-auto mb-5" /><b>در حال اتصال به اتاق...</b></div></div> : <section className="glass-panel min-h-[420px] flex-1 rounded-[2rem] p-3 sm:p-5"><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{all.map((p, i) => { const local = p.id === self?.id, s = local ? 'connected' : statuses[p.id], isMuted = local ? muted : p.isMuted; return <article key={p.id} className={`participant-card ${local ? 'is-self' : ''}`}><div className="card-top"><span className="role-label">{local ? 'شما' : i === 1 ? 'مهمان' : 'عضو'}</span></div><div className={`avatar ${local ? 'avatar-self' : ''} ${isMuted ? 'is-muted' : ''}`}><span>{initials(p.name)}</span>{!isMuted && s === 'connected' && <span className="speaking-ring" />}</div><div className="mt-4 truncate text-sm font-bold">{p.name}</div><div className="mt-1 flex items-center gap-1.5 text-[10px] text-white/35">{isMuted ? <MicOff className="h-3 w-3" /> : <Mic className="h-3 w-3 text-emerald-300" />}{isMuted ? 'میکروفون خاموش' : s === 'connected' ? 'متصل به Voice' : s === 'connecting' ? 'در حال اتصال...' : 'اتصال ناموفق'}</div></article>; })}</div></section>}</main><div className="fixed bottom-[84px] left-1/2 z-30 flex -translate-x-1/2 gap-1 rounded-full border border-white/10 bg-black/30 p-1.5 backdrop-blur-xl">{REACTIONS.map(e => <button key={e} onClick={() => sendReaction(e)} className="reaction-btn">{e}</button>)}</div><div className="glass-controls fixed bottom-0 left-0 right-0 z-40 px-3 pb-[max(12px,env(safe-area-inset-bottom))] pt-3"><div className="mx-auto flex max-w-xl items-center justify-center gap-2 sm:gap-3"><button onClick={toggleMute} className={`control-btn primary ${muted ? 'danger' : ''}`}>{muted ? <MicOff className="h-5 w-5" /> : <Mic className="h-5 w-5" />}<span>{muted ? 'میکروفون خاموش' : 'میکروفون روشن'}</span></button><button onClick={toggleDeaf} className={`control-btn square ${deafened ? 'active' : ''}`}>{deafened ? <VolumeX className="h-5 w-5" /> : <Headphones className="h-5 w-5" />}</button><button onClick={() => setChatOpen(v => !v)} className="control-btn square"><MessageCircle className="h-5 w-5" /></button><button onClick={onLeave} className="control-btn leave"><LogOut className="h-5 w-5" /><span className="hidden sm:inline">خروج</span></button></div></div>{floating.map(x => <div key={x.id} className="floating-reaction" style={{ left: `${x.xOffset}%` }}>{x.emoji}</div>)}{chatOpen && <><button aria-label="بستن چت" onClick={() => setChatOpen(false)} className="fixed inset-0 z-50 bg-black/45" /><aside className="chat-drawer fixed bottom-0 left-0 top-0 z-[60] flex w-full max-w-[390px] flex-col border-r border-white/10 bg-[#0b1020]/95 shadow-2xl backdrop-blur-3xl"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><b className="flex items-center gap-2"><MessageCircle className="h-4 w-4 text-indigo-300" /> چت اتاق</b><button onClick={() => setChatOpen(false)} className="icon-btn"><X className="h-4 w-4" /></button></div><div className="flex-1 overflow-y-auto p-4">{messages.map(m => <div key={m.id} className="mb-4"><div className="mb-1 text-[10px] text-white/35">{m.senderName}</div><div className="rounded-2xl border border-white/10 bg-white/[.06] px-3.5 py-2.5 text-xs leading-5">{m.text}</div></div>)}<div ref={messagesEnd} /></div><form onSubmit={sendChat} className="border-t border-white/10 p-3"><div className="flex gap-2 rounded-2xl border border-white/10 bg-black/20 p-1.5"><input value={text} onChange={e => setText(e.target.value)} className="min-w-0 flex-1 bg-transparent px-2 text-sm outline-none" placeholder="پیام..." /><button className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-500 disabled:opacity-30" disabled={!text.trim()}><Send className="h-4 w-4 rotate-180" /></button></div></form></aside></>}</div>;
}
