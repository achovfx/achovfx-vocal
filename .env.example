# Required for multi-instance/serverless deployments (Vercel, etc.).
# Create a free Upstash Redis database and copy these values to .env.local / Vercel.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# WebRTC uses public STUN/TURN servers configured in VoiceRoom.tsx; no paid API is required.
